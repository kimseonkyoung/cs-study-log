/*
 * [lv?] 문제이름 — 문제 URL
 *
 * 문제 요약:
 * 접근 방식:
 * 시간복잡도:
 *
 * 실행 방법: java lv1-문제이름.java
 *   (JDK 11+ 단일 파일 실행 — .class 파일 안 생기고 바로 실행됨)
 *
 * 클래스명 규칙: 파일마다 고유한 영문 이름 (예: CaesarCipher, DescSort).
 *   전부 Solution으로 하면 같은 폴더에서 클래스가 중복돼 IDE가 충돌 낸다.
 *   프로그래머스 제출 시에는 사이트의 class Solution 틀에 solution 메서드만 복사.
 * 파일명에 하이픈이 있어도 클래스에 public만 안 붙이면 컴파일된다.
 */
class ProblemName {
    public String solution(String s) {
        // TODO: 여기에 풀이 작성
        return "";
    }

    public static void main(String[] args) {
        ProblemName sol = new ProblemName();
        // 로컬 테스트 케이스 — 프로그래머스 제출 전에 여기서 검증
        System.out.println(sol.solution("테스트입력") + " | 기대값: ???");
    }
}

/*
 * ── 막혔던 부분 / 실수 ──
 * (풀고 나서 기록)
 */
